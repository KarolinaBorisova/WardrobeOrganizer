@model InfoHouseViewModel
@{
    ViewData["Title"] = "House Info";
}
<h2 class="text-center">@ViewData["Title"]</h2>
<h2 class="text-center">House Name: @Model.Name</h2>
<h2 class="text-center">Address: @Model.Address</h2>
<div class="text-center">
    <h5 class="display-5">Family houses</h5>
</div>
@if (Model.Storages.Count == 0)
{
    <div class="text-center">
        <h5 class="display-8">There are no storages yet.</h5>
    </div>
}
else
{
    <div class="card-group">
       
        @foreach (var storage in @Model.Storages)
        {
            <div class="card">
                <div class="card-body">
                    <h4><a class="card-title text-dark" asp-controller="Storage" asp-action="Info">@storage.Name</a></h4>
                </div>
            </div>
   
        }
    </div>
    <div class="text-center">
        <a class="btn btn-primary" asp-controller="Storage" asp-action="All">Show all house storages</a>
    </div>
}

<div class="text-center">
    <a class="btn btn-primary" asp-controller="Storage" asp-action="Add">Add new storage</a>
</div>